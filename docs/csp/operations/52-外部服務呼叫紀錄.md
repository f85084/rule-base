# 外部服務呼叫紀錄

本篇是 CSP `/ApiLog` 外部服務呼叫紀錄查詢入口。此頁只讀取 `apilog` 的請求與回應資料，供維運依時間、分頁及內容追查串接問題；不提供刪除、重送或修改外部呼叫的操作。

## 敘述狀態與查核界線

- **現行事實**：`/ApiLog` 的選單入口、查詢／翻頁用途、欄位判讀與本頁不提供刪除／重送／修改操作，依目前頁面與來源整理。
- **歷史規則**：需求與既有手冊中的欄位或清理描述只代表來源文件曾如此記載；不能據此增加本頁未確認的外部服務控制功能。
- **推論**：以 RequestId、時間與上下游紀錄交叉比對，是維運排錯的判讀方法，不等於已證明外部服務一定收到或完成請求。
- **runtime 未確認**：頁面權限、`apilog` 實際可見範圍、查詢回應排序與底層例外處理未在本次環境驗證；以當期 UI、回應、設定與 log 為準，敏感 request／response 不得任意外傳。

## 1. 權限與查詢範圍

- 登入後可查詢；實際頁面可見範圍仍以目前帳號與環境為準。
- 查詢條件是請求時間區間與分頁，不要把此頁當成外部服務的即時健康檢查或重送入口。
- request／response 內容可能包含串接資料。複製或回報時只保留必要識別資訊，依既有維運資料處理規範保護敏感內容。

## 2. 查詢與翻頁

1. 開啟 `/ApiLog`，先確認目前環境與查詢目的。
2. 設定 `startTime`、`endTime`；只需要單側範圍時可保留另一側為空。
3. 執行查詢，依清單查看呼叫名稱、識別碼、目標位置、請求／回應內容與時間。
4. 以頁碼與每頁筆數翻頁；翻頁時保留原本的時間條件，避免把不同範圍的結果混在一起。
5. 追查串接問題時，以 RequestId、ApiName、RequestTime 對照上下游紀錄，再判斷是否需要轉交服務負責人。

查詢完成後先依時間與頁碼核對清單，再以識別碼對照上下游紀錄；技術入口與欄位映射集中在下方的維運查核補充。

## 維運查核補充

以下 API、欄位與資料來源只供維運查核；一般讀者先依前文畫面步驟操作。查詢權限、可見範圍與底層例外尚未以 runtime 驗證，不是現行契約。

查詢入口為 `GET api/ApiLog/QueryApiLog`，參數是 `startTime`、`endTime`、`page`、`pageSize`；後端分別查詢總筆數與當頁清單，結果依 `Id` 遞減排列。

### 欄位與規則

| 欄位／資料 | 維運用途 |
|---|---|
| `startTime`／`endTime` | 以請求時間篩選；任一為空時忽略該側邊界。 |
| `page` | 分頁頁碼；小於或等於 0 時偏移量按 0 處理。 |
| `pageSize` | 每頁筆數；需為有效整數。 |
| `ApiName` | 辨識呼叫的 API。 |
| `RequestId` | 串接上下游紀錄的交叉比對鍵。 |
| `RequestUrl`、`RequestData` | 確認實際請求目標與內容。 |
| `RequestTime`、`ResponseTime` | 對照請求與回應時間。 |
| `ResponseData` | 確認外部回應內容；回報時避免暴露不必要資料。 |

查詢條件完全為空時，會依分頁查詢全量資料；資料量較大時先縮小時間範圍再追查。

## 4. 異常處理

| 情況 | 處理方式 |
|---|---|
| 查無資料 | 確認時間區間、環境、頁碼與每頁筆數；空集合不代表可以自行補造紀錄。 |
| 翻頁結果不符預期 | 檢查是否保留原時間條件，以及是否將 page 設為有效值；重新查詢後再判斷。 |
| 查詢失敗或底層例外 | 記錄時間區間、帳號、環境與錯誤訊息；交由系統／資料庫維運追查。 |
| 找不到預期外部呼叫 | 先確認呼叫時間、查詢環境及實際呼叫名稱；不要以此頁沒有資料推論外部服務未被呼叫。 |

## 5. 需求單與來源

- [外部服務呼叫紀錄需求](../requirements/pages/52-外部服務呼叫紀錄.md)：2 筆需求單，頁面路由 `/ApiLog`。
- 代表需求：[#49163 清除作業排程：apilog](../../../data/sources/csp/redmine/issues_all.json)、[#34893 外部服務呼叫紀錄](../../../data/sources/csp/redmine/issues_all.json)。
- [CSP 排錯資料](../troubleshooting/README.md)、[MI 完整手冊](MI完整手冊.md)。
- [網站選單來源](../../../data/sources/csp/website-menus.json)、[Redmine 來源快照](../../../data/sources/csp/redmine/issues_all.json)。
