# 訂單質檢

## 一般人員操作入口

**這頁做什麼**：在 `/OrderQA` 查詢並處理訂單質檢，核對訂單、音檔、質檢項目、缺失／懲處、上訴與複核。適用質檢人員與主管；一般客服只在被交辦且有權限時進入。

**開始前**：準備案件／訂單、客代、質檢日期、目前處理人員與必要音檔／Connection ID；先確認帳號可處理的案件範圍。

**照著做**：

1. 進入【訂單質檢】查詢，開啟案件後核對基本資料、訂單、音檔、狀態與歷程。預期結果：案件與通話對得上。
2. 逐項閱讀質檢內容，填分數、結果、處理方式與說明；不要只寫備註而略過正式欄位。預期結果：表單驗證通過。
3. 判斷正常／缺失，依畫面流程送出；需要上訴／複核時保留原判斷與附件。預期結果：案件節點與歷程更新。
4. 結案前回看結果、分數、處理人員、附件與下一節點。預期結果：沒有待處理節點。

**完成確認與常見問題**：質檢結果、歷程、案件狀態與關聯音檔可回查。案件空白先核對日期／權限；單一音檔或附件失敗分開排查；送出結果不明不要重複判定。

**影響與技術附錄**：送出、懲處、上訴、複核、匯入與重新指派會寫入質檢流程；測試資料、API 與欄位規則留在後段。

## 敘述狀態與查核界線

- **現行事實**：本頁正文依目前列出的頁面用途、操作流程與來源整理。
- **runtime 未確認**：未在目前環境實際驗證的角色按鈕、API 回應、資料同步、保存期限與版本差異，不視為目前保證；以當期 UI、API 與授權設定為準。

本頁給質檢人員、主管與維運人員查詢及處理訂單質檢案件使用。網站路由為 `/OrderQA`；需求單與來源請見文末連結。

## 1. 先確認角色與案件

訂單質檢涉及質檢人員、組長、處長及質檢小組主管等角色。開始處理前先確認：

1. 目前登入帳號的質檢權限與可處理範圍。
2. 案件所屬專員、目前質檢者與目前處理人員。
3. 質檢類型、質檢日期、案件狀態，以及是否已有上訴或複核紀錄。
4. 關聯訂單與音檔是否正確；若需要追查通話，先確認音檔與 Connection ID。

質檢歷程涉及權限控管；一般權限查詢範圍也可能受時間限制，需依目前帳號權限與頁面提示判斷。

## 2. 查詢與開啟質檢單

1. 進入【訂單質檢】頁面，依可用條件查詢案件。
2. 開啟案件後，先閱讀質檢單基本資料、關聯訂單、音檔與既有歷程。
3. 確認目前流程節點與處理人員，再開始填寫或判斷質檢內容。
4. 若需查核通話內容，使用音檔或 Connection ID 對照實際紀錄；不要只依訂單摘要判斷。

## 3. 判斷質檢結果

依質檢項目逐項判斷，並保留必要的處理方式、分數、說明與附件。分數不可填寫負數；批次匯入時也要確認欄位格式、CID、質檢類型、被懲處人及處理方式是否正確。

### 正常流程

判斷為正常時，依目前流程送出。若兩位組長均判斷正常，案件可正常結案；測試資料預期 `order_qa.resultType` 為 `2`。

### 缺失與懲處流程

判斷有缺失時，依案件流程選擇懲處與處理方式，再送出確認。系統會依流程保存缺失判斷與處理人員；不要只在備註描述缺失而未完成正式結果欄位。

若同一案件有不同組長的判斷，需確認系統依流程保存的缺失組長員編。自動判斷案例中，第一位組長判斷正常、第二位組長判斷懲處時，預期 `order_qa.mistake` 記錄第一位組長員編。

## 4. 上訴與複核

1. 確認案件是否符合上訴條件，以及目前是否仍在上訴時間內。
2. 上訴時保留上訴內容、原判斷、相關附件與處理歷程。
3. 若案件進入複核，依複核流程完成判斷後再結案；不要直接覆蓋原有判斷。
4. 處理完成後回看歷程，確認上訴、複核、處理人員與時間均已寫入。

質檢歷程與附件都有權限限制；若看不到資料，先確認帳號權限與案件歸屬，不要直接判定資料不存在。

## 5. 批次匯入與重新指派

### 批次匯入

批次匯入前先確認 Excel 欄位與資料格式，至少核對：

- CID／關聯訂單資料。
- 質檢類型。
- 被懲處人與員工編號。
- 處理方式、分數、說明及附件／截圖欄位。
- 員工是否已離職或留停。

匯入失敗時保留完整錯誤訊息，特別檢查 CID、欄位換行與員工狀態；不要只重複匯入同一份檔案。

### 重新指派

需要變更質檢人員或處理人員時，先確認質檢日期、部門與角色條件，再依權限重新指派。重新指派後應回看案件歷程與目前 `assignTo`，確認案件沒有留在原處理人員名下。

## 6. 結案前檢查

結案前逐項確認：

1. 質檢項目已完成判斷，分數與處理方式正確。
2. 缺失、懲處或正常結果已依流程送出。
3. 關聯訂單、音檔、Connection ID 與附件對應正確。
4. 上訴／複核案件已完成必要流程，歷程可追溯。
5. 案件狀態已更新，且沒有待處理人員或下一流程節點。

## 7. Lab 驗證流程

訂單質檢的 Lab 測試請分別依下列兩份文件執行：

1. 先準備[訂單質檢測試資料](../test-data/test-data-order-qa.md)，包含測試帳號、`order_qa` 初始化欄位與 Lab 主管設定。
2. 再依[訂單質檢測試案例](../test-cases/test-cases-order-qa.md)執行流程與驗證結果。

測試案例只維護在 `test-cases` 文件；本操作頁保留使用流程與入口，不重複列出案例表。

## 維運查核補充

本節集中質檢單、上訴、批次匯入、重新指派與歷程寫入的技術查核範圍；一般讀者先依前文案件流程操作。批次部分成功、權限與後續通知若未 runtime 驗證，以回應與歷程為準。

## 需求單與來源

- [訂單質檢需求](../requirements/pages/24-訂單質檢.md)：60 筆需求單。
- [訂單質檢測試資料](../test-data/test-data-order-qa.md)
- [訂單質檢測試案例](../test-cases/test-cases-order-qa.md)
- [測試資料總索引](../test-data/test-data.md)
- [MI 完整手冊](MI完整手冊.md)
- 原始來源：`data/sources/csp/訂單質檢.pdf`
