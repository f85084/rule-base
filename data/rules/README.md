# Rules

此目錄未來存放 RuleBase 的規則與領域知識結構，例如：

- 領域實體（DomainEntity）定義
- 商業規則（BusinessRule）描述
- 文件片段（DocumentChunk）與向量索引設定

目前測試資料先以 Markdown 表格整理在 `docs/test-data.md` 及其拆分文件。
未來若需要機器可讀的規則格式，再將此目錄擴充為 schema / entity / business-rule 結構。
